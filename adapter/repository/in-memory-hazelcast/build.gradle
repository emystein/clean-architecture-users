plugins {
	id 'org.jetbrains.kotlin.jvm'
}

ext.moduleName = 'slalom.example.db.hazelcast'

dependencies {
	implementation(
		project(':domain'),
		project(':usecase'),
		'com.hazelcast:hazelcast:3.9.3',
	)
}
